/**
 * http://usejsdoc.org/
 */
// catch the object from another file, mention th ename
// use require to inherit th eproperty of another file
// catch it in a variable

var obj = require("./JsObjectDemo.js");

obj.getModal();
console.log(obj.engine);